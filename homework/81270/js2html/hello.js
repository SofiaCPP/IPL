// Single line comment

const someVariable = 3.14e+2;
const someString = 'Escaped \' string!';
const someTemplate = `A simple one`;
const someRegex = /[0-9]+/i;

/**
    Multiline comment
 */
function foo() {
    return [] instanceof Array;
}