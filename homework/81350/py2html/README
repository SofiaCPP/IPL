To build the program run the bash script build_lexer.sh:
$ ./build_lexer.sh

To run the highlither at a given program:
$ ./py2html <program name>
When you run the program with the highlither, it will produce html
file at the current directory named index.html.

There is a test program named program.py:
$ ./py2html program.py


