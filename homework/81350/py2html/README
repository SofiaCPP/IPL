To build the program run the bash script build_lexer.sh:
$ ./build_lexer.sh

To run the highlighter on a given program:
$ ./py2html <program name>
When you run the highlighter, it will produce an html
file in the current directory named "index.html".

There is a test program named program.py:
$ ./py2html program.py


